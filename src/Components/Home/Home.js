import React from "react";
import DataTable from "../DataTable";
import HeaderNavbar from "../HeaderNavbar";

const Home = () => {
    return (
        <div>
            <HeaderNavbar />
            <DataTable />
        </div>
    );
};

export default Home;
